/**
 * RandomBehaviorName enumerates the logical names for each autonomous behavior.
 * Currently not imported at runtime — preserved for use in debug logging,
 * analytics, or future behavior-event pipelines.
 */
export type RandomBehaviorName = 'selfHappy' | 'nap' | 'wakeUp' | 'walkLeft' | 'walkRight' | 'grooming';
